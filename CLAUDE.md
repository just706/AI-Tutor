# AI Tutor 项目开发宪法

本文件用于约束 AI Tutor 项目中的 AI 编码助手和开发者行为，目标是减少大语言模型在编码、重构、文档编写和架构设计中的常见错误。

本宪法优先强调：**稳妥、清晰、局部、可验证**。

如本文件与用户的明确指令冲突，以用户的明确指令为准；如与项目更具体的模块规范冲突，以更具体的规范为准。

---

## 1. 基本原则

### 1.1 稳妥优先

在 AI Tutor 项目中，任何改动都应优先保证系统稳定性，而不是追求开发速度。

处理简单任务时可以保持轻量，但不得因为追求速度而跳过必要的确认、验证和风险说明。

### 1.2 不主观假定

编码或修改文档前，必须先理解真实需求。

不得在需求不明确时擅自假定业务含义、接口行为、数据库结构或 AI 模型调用方式。

### 1.3 目标驱动

所有改动都必须服务于明确目标。

每一处代码、配置、文档修改，都应能对应到用户提出的需求或项目已经确认的设计。

---

## 2. 编码前先思考

动手实现前，应先完成以下判断：

- 明确本次任务的目标。
- 明确本次任务的验收标准。
- 明确哪些内容在范围内，哪些内容不在范围内。
- 明确当前实现依赖哪些已有代码、配置、接口或数据库结构。
- 明确是否存在多种实现方案。

如果需求存在多种解读方式，必须列出可能解释，不得擅自选择其中一种。

如果存在不确定之处，应主动提问或明确说明假设。

如果有更简单、更稳妥的实现方式，应主动说明方案取舍。

---

## 3. 优先追求简洁

项目实现应坚持最小可用原则。

### 3.1 不做额外功能

不得实现需求之外的功能，例如：

- 用户没有要求的复杂配置能力。
- 用户没有要求的通用框架封装。
- 用户没有要求的多模型调度策略。
- 用户没有要求的复杂异常分支。
- 用户没有要求的 UI 动效或额外页面。

### 3.2 不过度抽象

仅单次使用的逻辑，不应过早抽象成工具类、框架层或复杂设计模式。

如果简单代码可以清晰解决问题，应优先使用简单实现。

### 3.3 控制代码规模

如果一个需求可以用较少代码清晰完成，不应写出大量复杂实现。

检查标准：

```text
资深工程师是否会认为这段代码过度复杂？
```

如果答案是肯定的，应重新简化。

---

## 4. 精准局部修改

修改已有代码时，必须坚持 surgical changes，即精准局部改动。

### 4.1 只改必要内容

不得因为看到相邻代码不够优雅，就顺手重构、格式化或优化。

不得修改与当前任务无关的：

- 文件结构。
- 变量命名。
- 方法签名。
- 注释风格。
- 格式排版。
- 业务流程。

### 4.2 遵循现有风格

应遵循项目已有代码风格，即使存在个人更偏好的写法。

包括但不限于：

- 包结构。
- 命名习惯。
- Controller、Service、Mapper 分层方式。
- DTO、VO、Entity 的使用方式。
- 前端组件组织方式。
- 错误响应格式。

### 4.3 只清理本次产生的冗余

如果本次改动导致导入、变量、函数或配置失效，应清理这些由本次改动产生的 orphans。

但不得擅自删除原本就存在的 dead code，除非用户明确要求。

发现无关废弃代码时，可以在总结中提示，但不要直接删除。

---

## 5. 以验收标准驱动执行

每项任务都应转化为可验证目标。

### 5.1 Bug 修复

修复 Bug 时，应优先复现问题，再进行修复。

推荐流程：

```text
复现问题 → 定位原因 → 最小修复 → 验证通过 → 总结影响范围
```

### 5.2 新功能开发

新增功能时，应先明确输入、输出、数据流和验收标准。

推荐流程：

```text
确认需求 → 设计最小方案 → 实现核心功能 → 添加必要测试 → 验证结果
```

### 5.3 重构任务

重构不应改变外部行为。

推荐流程：

```text
确认原有行为 → 小步修改 → 保持接口兼容 → 运行测试 → 确认行为一致
```

---

## 6. AI Tutor 项目专项规范

AI Tutor 项目涉及 Spring Boot、Vue3、大模型调用、RAG 知识库和学习数据分析，应额外遵守以下规则。

### 6.1 AI 调用规范

大模型调用必须通过统一 AI 服务层封装，业务代码不应直接依赖具体模型供应商。

应避免把以下内容硬编码在业务逻辑中：

- API Key。
- 模型名称。
- 供应商地址。
- Prompt 大段文本。
- Token 限制参数。

### 6.2 Prompt 规范

Prompt 应保持清晰、可维护、可测试。

Prompt 设计应明确：

- AI 角色。
- 用户背景。
- 任务目标。
- 输出格式。
- 回答边界。
- 禁止编造无依据内容。

### 6.3 RAG 规范

RAG 模块中，LangChain4j 可作为流程编排框架，但不应把它误认为大模型或向量数据库。

职责划分应清晰：

- LangChain4j：负责文档加载、切片、检索编排和 Prompt 组装。
- Embedding 模型：负责生成向量。
- 向量数据库：负责向量存储和相似度检索。
- 大模型：负责基于检索内容生成回答。
- MySQL：负责保存文档元数据和业务数据。

RAG 回答应优先基于检索资料；资料不足时应明确说明，不得编造。

### 6.4 数据库规范

数据库设计应避免以下问题：

- 明文存储密码。
- 用户数据未按用户 ID 隔离。
- 聊天记录无会话 ID。
- 文档向量直接塞入普通业务表。
- 关键查询字段缺少索引。

涉及用户、学习记录、文档和聊天数据时，应优先考虑数据归属和访问权限。

### 6.5 前端规范

前端实现应优先保证清晰可用。

AI 聊天、教学模式、文档上传等页面应重点处理：

- 加载状态。
- 错误提示。
- 空数据状态。
- Markdown 内容展示。
- 长文本阅读体验。

不得为了视觉效果引入过重依赖或复杂交互。

---

## 7. 安全与隐私原则

### 7.1 用户数据安全

用户只能访问自己的学习档案、聊天记录、答题记录和上传资料。

任何查询用户数据的接口都必须校验当前登录用户身份。

### 7.2 密码安全

密码不得明文存储。

应使用安全哈希算法保存密码，例如 BCrypt。

### 7.3 API Key 安全

大模型 API Key、数据库密码等敏感配置不得提交到代码仓库。

应通过环境变量、配置中心或本地私有配置文件管理。

### 7.4 文件上传安全

文件上传必须限制：

- 文件类型。
- 文件大小。
- 存储路径。
- 访问权限。

上传文件不应直接作为可执行内容使用。

---

## 8. 文档规范

项目文档应与实际设计保持一致。

核心文档包括：

- `docs/PRD.md`：产品需求。
- `docs/Architecture.md`：系统架构。
- `docs/Database.md`：数据库设计。
- `docs/API.md`：接口文档。
- `docs/AI-Design.md`：AI 模块设计。
- `docs/Deployment.md`：部署文档。
- `docs/Development.md`：开发计划。

修改接口、数据库、AI 流程或部署方式时，应同步更新相关文档。

不得让文档声称已经完成实际尚未实现的功能。

---

## 9. 禁止行为

在本项目中，以下行为默认禁止：

- 未确认需求就大范围重构。
- 为简单需求引入复杂框架。
- 擅自删除无关代码。
- 擅自修改数据库表设计但不说明影响。
- 在代码中硬编码密钥。
- 明文保存用户密码。
- 绕过权限校验访问用户数据。
- AI 回答中编造资料来源。
- RAG 检索不到资料时仍假装基于资料回答。
- 修改与当前任务无关的格式和风格。

---

## 10. 完成定义

一个任务只有同时满足以下条件，才算完成：

- 实现内容符合用户需求。
- 改动范围尽量小且可解释。
- 没有引入需求之外的额外功能。
- 已清理本次改动产生的冗余代码。
- 必要测试或验证已通过。
- 关键风险、假设和未完成事项已说明。
- 相关文档已同步更新。

---

## 11. 术语说明

| 术语 | 含义 |
|---|---|
| surgical changes | 精准局部改动 |
| orphans | 因修改产生的冗余失效代码 |
| dead code | 死代码、废弃代码 |
| tradeoffs | 方案取舍、权衡 |
| abstractions | 抽象封装 |
| RAG | 检索增强生成 |
| Prompt | 提示词 |
| Agent | 能够围绕目标进行规划和执行的 AI 组件 |
